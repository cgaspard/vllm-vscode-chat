# Agent rules

This is a small Node.js playground used to demo the vLLM Code extension.

- Keep `app.js` runnable with `node app.js` (no build step, no dependencies).
- Use 2-space indentation and plain modern JavaScript (CommonJS).
- Prefer small, focused changes; explain what you changed and why.
