.vscode/**
.vscode-test/**
.claude/**
.github/**
node_modules/**
out/**
out-test/**
src/**
test/**
**/*.ts
**/*.map
esbuild.js
esbuild.test.js
tsconfig.json
tsconfig.test.json
.gitignore
.vscodeignore
*.vsix
scripts/**
media/sample.gif
media/vllm-logo.svg
media/screenshots/**
releasenotes/**
*HANDOFF.md
# Internal/working docs: only README.md ships. Without this, ANY root markdown
# (prototype status, design notes, parity reports) lands in the .vsix on the
# Marketplace — PROTOTYPE-STATUS.md was doing exactly that.
*.md
!README.md
sample-workspace/**

# Local agent/tool config and generated code-graph output. These are untracked,
# so a CI build from a fresh checkout never sees them — but a local
# `vsce package` would happily publish your absolute paths and an extracted
# copy of the source. Keep them out regardless of who is packaging.
.codex/**
.mcp.json
opencode.json
graphoxide-out/**
